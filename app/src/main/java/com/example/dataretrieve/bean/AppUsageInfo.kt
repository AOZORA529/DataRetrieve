package com.example.dataretrieve.bean
//import kotlin.collections.MutableList

class AppUsageInfo (var timeForeground: Long,
                    var timestampOpen: MutableList<Long>,
                    var timeDurationAggregated: MutableList<Long>,
                    var countOpen: Int){

    // 前台运行总时间

    // 列表，存储每一次打开的时间点

    // 列表，存储每一次打开过后的持续时间

    // 打开次数
}